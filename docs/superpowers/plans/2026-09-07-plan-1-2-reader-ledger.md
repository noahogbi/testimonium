# Plan 1.2 (the reader) - execution ledger

Preserved from `.superpowers/sdd/2026-09-07-plan-1-2-reader/progress.md`, which is
git-ignored scratch and does not survive worktree cleanup, as plans 1 and 1.1
preserved theirs. It is the record of what was decided and why: rulings R1-R13
with their cost-if-wrong, the per-task review findings and fix rounds, the final
whole-branch review's proof that every verdict this plan moves, moves out of
`unreachable`, and what was parked - including the N3 twin of the spec 6.3
correction (R13), which the next plan's pre-flight must take first.

The whole-branch review report and the fix brief it produced were read in full at
the time; their substance is in the ledger lines below. Read this alongside
`2026-09-06-plan-1-core-ledger.md` and `2026-09-07-plan-1-1-extraction-ledger.md`
before touching this repo.

---

# SDD ledger — plan: docs/superpowers/plans/2026-09-07-plan-1-2-reader.md

Worktree C:\Users\noaho\testimonium-plan12, branch feat/plan-1-2-reader, start 9854337 (main @ 3974d27 + spec amendment ae9d299 + plan 9e2602f + Fable corrections 9854337). Spec: docs/superpowers/specs/2026-09-06-testimonium-design.md section 6.6 is the binding authority. 246 tests green at start. Execution approved by the user 2026-09-08. Push/merge/publish need separate explicit authorisation.

## Pre-flight conflict scan (2026-09-08, before Task 1)

Pairs sharing a file or interface:

| Tasks | Produces vs consumes | Found |
|---|---|---|
| T1 -> T2, T3, T4 | `isReadable(Pick<Signals,...>)` from verdict.ts; T2 read-source calls `isReadable(computed.signals)`, T2 Step 11 reachability calls `isReadable(c.signals)`; T4 uses it only via `bestReadable` | agree |
| T2 -> T3 | `readSource(url, [], {fetcher, rules?})` -> `{reads, attempted, pdfUrl}`; `bestReadable(reads)` -> `Read \| undefined` with `.rung`, `.computed.signals.proseChars`; T3 replaces reachability.ts imports (lines 1-8) wholesale, dropping the `isReadable` T2 Step 11 added | agree |
| T2 -> T4 | T2 writes `import { readSource, type Read }`; T4 replaces that line with `import { bestReadable, readSource, type Read }` | agree |
| T2 ladder `Attempt {rung, readable}` vs its two pushers | check.ts push is inside the block Step 10 deletes; reachability.ts:88 push rewritten in Step 11 | agree; steps 5-9 leave check/reachability non-compiling until 10-11, plan says so; vitest (esbuild) runs read-source tests without typechecking check.ts |
| T2 check.ts import trim | drops computeSignals, nextAction, Attempt, isPdf, EMPTY_RESPONSE, RawResponse, RungId; verified at 9854337 every remaining use of those names is inside lines 69-117 (the replaced block); `attempted`, `pdfUrl`, `fetcher.rungs` used at 122-123/173/181 come from the destructured result | agree |
| T2 check.test.ts (3 pins) -> T4 check.test.ts (3 more) | T4 says add no imports; `THRESHOLDS`, `proseVolume`, `toText` at lines 3-4 of check.test.ts at 9854337 | agree |
| T2 exports.test pin -> T7 exports.test VERSION pin | T7 uses `readFileSync`, already imported at line 2 | agree |
| T5 excerpt.ts (foldWithMap, ~70-99) vs T6 excerpt.ts (DROP, 36-46) | disjoint regions; T6 Step 7 expects 57 non-ASCII, which T5 Step 4 verifies it preserved | agree |
| T5 vs T6 test/text/excerpt.test.ts | both append tests; no shared names | agree |
| T6 README (delete bullet 250-260) -> T7 README (279-287 after T6, 290-298 before) | plan accounts for the 11-line shift (Fable F6); T7 anchors on text | agree |
| T6 calibration doc (+ closing note) -> T7 byte check expecting calibration 165 non-ASCII | T6's inserted text is ASCII; T6 Step 11 checks 165 itself | agree |
| T4 Step 3 source comment vs Global Constraints licence line | comment says rule 2 is "the one place plan 1.2 moves a verdict toward accusation"; Global Constraints (Fable F1) say changes (1) and (2) both do | CONFLICT -> Ruling R1 |
| T6 Step 11 dated doc text vs Global Constraint "every number from a command run that day" | inserted calibration text says "Closed 2026-09-07" / "counted 2026-09-07"; execution is on 2026-09-08 | CONFLICT -> Ruling R2 |

Per-task self-consistency:

| Task | Tests vs code vs files | Found |
|---|---|---|
| T1 | 4 tests on `isReadable`; Step 3 adds it after `isBlocked`, routes `verdict()`'s last line through it; Step 4 count: isBlocked( def + 4 calls (verdict.ts x2, check.ts:154, reachability.ts:80) - at 9854337 the grep shows def + 3 calls, so +1 from isReadable = 4 | agree; 246 -> 250 |
| T2 | 8 reader tests, 7 ladder tests (one deleted), 3 check pins (each fails on rungsAttempted first), 1 exports pin; 250+8+3+1-1 = 261 | agree |
| T3 | 3 reachability tests, exactly one (rule 5) fails first; 261+3 = 264; Step 5 grep proves the loop left | agree |
| T4 | 3 check tests (rule 2, gone-doc exposure, union survival) + 5 agreement rows; 264+8 = 272; Step 2's expected failures match the probes I ran at 3974d27 | agree |
| T5 | 2 tests; export first (TypeError), then repair; 272+2 = 274; byte count 57 | agree |
| T6 | 2 normalize + 1 excerpt tests; the excerpt test fails at the matcher until Step 5, then at DROP until Step 7; 274+3 = 277; two commits (code, docs) | agree, except the dates (R2) |
| T7 | 1 VERSION pin proven to fail by editing index.ts and restoring; README/CHANGELOG/spec edits; byte checks; 277+1 = 278 | agree |

Anchors verified at 9854337: check.ts:1-10 imports, :134-135 reducer, :146 locatedBy; reachability.ts:1-8, :88; ladder.ts:1, :34-35; excerpt.ts:95-96; normalize.ts:15; exports.test.ts:2.

Ruling: R1 - Task 4's source comment above `won` is written as "one of the two places plan 1.2 moves a verdict toward accusation (the other is the ladder's escalation in read-source.ts, which accuses only when the readable read is also the largest)" instead of "the one place" — the Global Constraints line was corrected by Fable F1 after the comment was drafted and spec 6.6 licenses both — costs a wrong code comment if I have misread the licence.
Ruling: R2 - Task 6 Step 11 writes the date the implementer actually ran the counting command (execution date, 2026-09-08) wherever the plan text says 2026-09-07 — the Global Constraint requires numbers from a command run that day — costs a one-day discrepancy against the plan text if the constraint is read as "the plan's date".

## Tasks
Task 1: implementer DONE (haiku) - commit a3da53c, 250 passed, typecheck clean, verdict.ts non-ASCII 0. Review dispatched (sonnet) on review-9854337..a3da53c.diff.
Task 1: complete (commits 9854337..a3da53c, review clean)
Task 2: implementer dispatched (sonnet), BASE a3da53c.
Task 2: implementer BLOCKED at Step 14 - typecheck TS2345 at src/check.ts:76: check()'s `claims: readonly string[]` cannot be passed to the brief's verbatim `readSource(url: string, claims: string[], ...)`. 261 tests pass; nothing committed.
Ruling: R3 - `readSource`'s second parameter is `claims: readonly string[]` (the plan's Produces block and Step 7 code said `string[]`) — readSource never mutates claims and only forwards them to computeSignals, which already takes `readonly string[]`; the wider parameter type is the only change that lets Step 10's verbatim call compile without touching check()'s public signature — costs nothing if wrong: a readonly parameter accepts every caller a mutable one does, including Task 3's `[]` literal.
Task 2: implementer DONE after R3 - commit 8a1d14c, 261 passed, typecheck clean, five source files non-ASCII 0. Review dispatched (opus) on review-a3da53c..8a1d14c.diff.
Task 2: review - Spec compliant, quality Approved, 0 critical / 0 important / 4 minor.
Task 2: minor (deferred): the three escalation pins sit after the "INVARIANT: an unsupported verdict always names at least one missed claim" test (test/check.test.ts:366-416), not after "never names a claim in `missed` that some rung located" as Step 1 anchored; no behavioural effect; task-2-report.md:9-11 misstates the placement.
Task 2: minor (deferred): task-2-report.md:98-104 heading claims the three pins were re-run in isolation for GREEN; body says they were not. GREEN is covered only by the 261-test run (adequate; evidence-hygiene defect).
Task 2: minor (deferred): `sourceLabel` crosses check -> readSource -> computeSignals with no test pinning the hop (only test/classify/corpus-verdict.test.ts:19 uses it, directly on computeSignals); dropping the option at either hop leaves the suite green. Candidate for the final fix dispatch: one assertion in test/fetch/read-source.test.ts that a sourceLabel raises reads[0].computed.signals.slugLabelOverlap.
Task 2: minor (deferred): src/fetch/types.ts:48-50 comment (plan-mandated) says a fetcher throw is caught in read-source.ts "under both check() and reachability()" — true of check only until Task 3 lands; Task 3's review must confirm the sentence became literally true.
Task 2: complete (commits a3da53c..8a1d14c, review clean of critical/important; 4 minors deferred)
Task 3: implementer dispatched (sonnet), BASE 8a1d14c.
Task 3: implementer DONE (sonnet) - commit c6977a7, 264 passed, typecheck clean. Review dispatched (sonnet) on review-8a1d14c..c6977a7.diff.
Task 3: review - Spec compliant, quality Approved, 0 findings. Reviewer proved rule 5 is a strict widening (OLD-readable implies NEW-readable; reads population unchanged since Task 2 already unified the stop rule) and that proseChars/rung reporting is identical where both rules agree. Task 2's deferred types.ts:48-50 comment is now literally true (resolved).
Task 3: complete (commits 8a1d14c..c6977a7, review clean)
Task 4: implementer dispatched (sonnet), BASE c6977a7. Carries R1.
Task 4: implementer DONE (sonnet) - commit 14d47a9, 272 passed, typecheck clean, check.ts non-ASCII 0, R1 applied. Review dispatched (opus) on review-c6977a7..14d47a9.diff.
Task 4: review (opus) - Spec compliant (byte-identical to brief except R1), quality Approved, 0 critical / 0 important / 5 minor. Reviewer's case analysis: with the two-rung ladder the only reachable transition is unreachable -> unsupported from a readable read (licensed change 2); none into unreachable or supported.
Task 4: minor (deferred): M1 - the "no move into supported" half of the licence is non-local: it rests on HTML_ORDER being two rungs (src/fetch/ladder.ts:17). With a third HTML rung, [vetoed wall over floor and largest, non-vetoed sub-floor read matching A, readable read matching B] would go old unreachable -> new supported via the cross-read union (check.ts:123-136). Nothing in check.ts records the dependency. Candidate for the final fix dispatch: one sentence in the Rule 3 comment or beside HTML_ORDER. Also a lens for the final reviewer.
Task 4: minor (carry to Task 7): M2 - the ACCEPTED EXPOSURE is wider than the gone-document pin's framing: rule 2 accuses from ANY readable later read when the first read is vetoed and at least as large (soft-404 landing page, post-removal redirect target, mirror homepage); C1 never gates (verdict.ts:103-105). The README disclosure must say "any readable read reached after a vetoed one is judged on its own", not just chrome-at-200.
Task 4: minor (carry to Task 7): M3 - test/agreement.test.ts:6-12 cites `README, "Commands"` for a statement the README does not yet contain; README:290-297 still says the preflight ORs vetoes across rungs ("verified one-directional") - now false in both directions. Task 7 must rewrite that bullet or the test's citation is false.
Task 4: minor (carry to Task 7): M4 - firedRule is provenance of `won`; under rule 2 a walled first rung leaves no trace on an `unsupported` result except rungsAttempted. Correct by io/evidence.ts:91's contract; worth a line in the disclosure.
Task 4: minor (deferred): M5 - R1's comment points at read-source.ts for the escalation decision that lives in ladder.ts:37-45 (read-source.ts:78 supplies the readable bit). Accurate enough; a reader lands in the one ladder loop.
Task 4: complete (commits c6977a7..14d47a9, review clean of critical/important; 5 minors deferred/carried)
Task 5: implementer dispatched (haiku), BASE 14d47a9.
Task 5: implementer DONE (haiku) - commit 97cb456, 274 passed, excerpt.ts non-ASCII 57. Review dispatched (sonnet) on review-14d47a9..97cb456.diff.
Task 5: review (sonnet) - Spec compliant, quality Approved, 0 critical / 0 important / 1 minor. Reviewer reproduced 59/60/28/27 from scratch and proved parity for every BMP code unit (none lowercases to length 0; all 11 FOLD entries are single-unit).
Task 5: minor (deferred, pre-existing): the U+10400 astral control passes incidentally - foldWithMap iterates per UTF-16 unit (excerpt.ts:74), so an astral character is never case-folded (output U+10400, not U+10428); grows 0 holds because each surrogate half passes 1:1. Not introduced by this task; the test's comment overstates why it passes. Park for plan 2 (harvest is the consumer).
Task 5: complete (commits 14d47a9..97cb456, review clean of critical/important; 1 minor deferred)
Task 6: implementer dispatched (sonnet), BASE 97cb456. Carries R2.
Task 6: implementer DONE (sonnet) - commits 3f74990 + b662fe5, 277 passed, typecheck clean; controller re-counted: normalize.ts 0 / excerpt.ts 57 / README 3 / calibration 165 non-ASCII, raw C2AD 0 in both sources (self-test 1), u00AD grep 1 / 2, README soft-hyphen mentions 0, R2 dates 2026-09-08. Review dispatched (sonnet) on review-97cb456..b662fe5.diff.
Task 6: review (sonnet) - Spec compliant (R2 applied exactly), 0 critical / 1 important (plan-mandated) / 0 minor. Reviewer proved the U+00AD deletion is match-monotonic (no input moves toward unsupported).
Task 6: important (plan-mandated): docs/calibration-2026-09.md:378-399 - the section body is still present tense ("`norm()` does not remove it", the "Measured directly" table says cooperation false, "Both spellings ... miss", "is parked for plan 2") and contradicts the new true/false table at :409-412 three paragraphs later. Step 11 only reworded the heading and appended the closure.
Ruling: R5 - the soft-hyphen section's body is recast into the past tense as a dated record of the ae9d299 measurement (`did not remove`, `were outside ... at ae9d299`, `Measured directly at ae9d299, before the change`, table header `phraseFound at ae9d299`, `missed`, `was an unsupported verdict`, `belonged with ... was parked for plan 2 and landed in plan 1.2, below`) rather than deleted - the entry is a dated ledger line and its history is the point; spec authority is the plan's own rule that every number in a doc is dated to its run, so a false-looking table must name its commit - costs a few tense edits in one doc section if wrong; nothing else changes.
Task 6: fix round 1 - resuming implementer (sonnet) with R5; third commit expected.
Task 6: fix round 1 - implementer DONE (sonnet), commit c83451c (calibration doc only, 8 lines, the five R5 replacements verbatim), 277 passed, typecheck clean, calibration non-ASCII 165 (controller re-counted 165). Scoped re-review dispatched (sonnet) on review-b662fe5..c83451c.diff.
Task 6: fix round 1 re-review (sonnet) - ADDRESSED; no new breakage; calibration non-ASCII 165, C2AD 0 (self-test 1). Out-of-scope minor (deferred): calibration:397 "changed which character breaks the match, not whether it breaks" is present tense inside the "Not a regression" comparison paragraph; framed by past tense on both sides, not a current-state claim. Leave unless another round touches the section.
Task 6: complete (commits 97cb456..c83451c, review clean of critical/important after one fix round; 1 minor deferred)
Ruling: R4 - Task 7's new README gone-document bullet gets one more sentence pair widening the disclosure beyond error chrome (Task 4 minors M2/M4): "The route is not specific to error chrome: any readable read reached after a vetoed one is judged on its own, whatever document it is - a soft-404 landing page, the target of a redirect after removal, a mirror's home page - because no signal the classifier gates on distinguishes the same page at 200 from a different document at 200 (slug overlap is computed and never gates). A wall on the first rung leaves no trace on such a result beyond `rungsAttempted`; `firedRule` is the winning read's." - the plan's bullet names one instance of a class the code cannot distinguish, and a disclosure narrower than the exposure is the drift class this repo is worst at; every clause is pinned (rule 2 test in test/check.test.ts: different document, not.toHaveProperty("firedRule"), rungsAttempted; test/classify/verdict.test.ts:70 slugLabelOverlap never gates) - costs two README sentences if wrong.
Task 7: implementer dispatched (sonnet), BASE c83451c. Carries R4 and Task 4 minors M2/M3/M4.
Task 7: implementer DONE (sonnet) - commit 6be6ad5, 278 passed, typecheck + build clean; controller re-counted README 3 / CHANGELOG 0 / spec 0 / calibration 165 / excerpt.ts 57 / index.ts 0 non-ASCII, 'can disagree' 0, R4 sentences present (README:230), diff -w stat == diff stat (87+/11-, 4 files). Review dispatched (opus) on review-c83451c..6be6ad5.diff - doc task, every sentence must name its pin; the drift class this repo is worst at.
Task 7: review (opus) - Spec compliant (all seven brief blocks + R4 verbatim), quality Approved, 0 critical / 1 important (pre-existing README text, not named by the brief) / 3 minor. Reviewer checked 20+ doc claims against code and pins, all TRUE; re-ran the isBlocked grep (definition + 4 calls).
Task 7: important: README.md:114-122 ("no model" bullet) still says "`unreachable` is decided from **seven** inputs" and "either one alone forces `unreachable`" - read-vs-citation confusion made false by Tasks 2/3 (a first read vetoed only by N4/N5 no longer forces the citation's verdict; test/check.test.ts:366,:381 assert supported) and contradicted by this task's own new line at README:205-207.
Ruling: R6 - fix it in this task's fix round rather than park it: the ledger task's contract is that the README is true after it, and the sentence names the tool's most consequential verdict. New text (all read-scoped, every clause pinned): "Whether a read is vetoed is decided from **seven** inputs" (verdict.ts isBlocked = five vetoes over those inputs; test/classify/verdict.test.ts), "either one alone vetoes the read" (N5 tests), plus one sentence tying the read to the citation: "A vetoed read is never judged on its own; the ladder climbs past it, and the citation reads `unreachable` only when no rung produced a readable read and none matched in full (spec 6.6 rule 3; the ladder is under Measured limits below)." - pinned by the N4/N5 climb tests (check.test.ts:366/381/393, read-source.test.ts, reachability.test.ts) and the rule 3 / sub-floor union pins (check.test.ts:283-308, :482). Costs three README sentences if wrong.
Task 7: minor (fix in the same round): README:308 blank line - the agreement text landed as its own paragraph; the brief said append to the paragraph ending "`reachability` needs neither." Delete the blank line.
Task 7: minor (deferred): CHANGELOG:19-20 "implemented once in src/fetch/read-source.ts" - the loop is there, the climb predicate is ladder.ts:41 (`if (last?.readable) return stop`); substance (one implementation, one nextAction caller) true. Leave.
Task 7: minor (deferred, for the record): CHANGELOG:11 "Three verdict-moving changes ... plus two repairs" uses the plan's vocabulary; the U+00AD repair also moves verdicts, unsupported -> supported only. Disclosure-safe. Leave.
Task 7: fix round 1 - resuming implementer (sonnet) with R6 + the blank-line minor; second commit expected.
Task 7: fix round 1 - implementer DONE (sonnet), commit 8dfe347 (README only, 8+/5-; R6 text verbatim, blank line removed), 278 passed, README non-ASCII 3 (controller re-counted 3; 'forces unreachable' 0). Scoped re-review dispatched (sonnet) on review-6be6ad5..8dfe347.diff.
Task 7: minor (deferred, cosmetic): README:126 'accepted set - any' is a ragged wrap left by the R6 insertion (the file already has one at 'with replacement characters and'); renders as one paragraph. Candidate for the final fix dispatch only if it touches that bullet anyway.
Task 7: fix round 1 re-review (sonnet) - both findings ADDRESSED (R6 text verbatim, traced TRUE against ladder.ts:38-39 / check.ts won / verdict.ts / spec rule 3; blank line removed), no new breakage, README non-ASCII 3.
Task 7: complete (commits c83451c..8dfe347, review clean of critical/important after one fix round; 3 minors deferred)
Final whole-branch review: dispatched (opus) on review-9854337..8dfe347.diff (code + docs; spec/plan commits ae9d299..9854337 named for the drift lens, spec 6.6 read at HEAD). Lens: a fourth verdict-moving case; breaking the keystone through rule 2; M1's third-rung scenario; the deferred minors.
Final whole-branch review (opus) - report at final-review-report.md. Verdict: Ready to merge WITH FIXES. Code is right (no executable change needed in check.ts / reachability.ts / read-source.ts / verdict.ts). Proof recorded: stopNew => stopOld, so the old read sequence is a prefix of the new one; won diverges only when the old winner was vetoed; EVERY verdict this branch moves, moves out of unreachable; no fourth verdict-moving case at two rungs; tie-breaks, union machinery, fetcher-throw path, PDF rung identical. Measured rows: E1 (node 404 chrome + curl 200 289-char stub with claim -> supported, UNPINNED), E3 (node octet-stream carrying the claim + curl clean other document -> unsupported), G1/G1' (node 404 vs 200, same bodies -> unsupported vs supported: the status is the but-for cause of the accusation).
Final review findings: 1 critical (spec 6.3:515-517 "forces unreachable ... a status can never supply an accusation" is false at HEAD: G1; the very paragraph that records having gone quietly false once before), 3 important (README :151-163 and :138-139 N5 bullets still in citation voice while :203-208 N4 bullet and :114-124 were read-scoped; README:231-232 R4 parenthetical makes a choice look like a limit - finalUrl captured and never compared, headMarkers computed and never gating, neither on CitationResult; E1 route unpinned), 8 minor (5 ladder.ts two-rung bound unrecorded; 6 check.ts:102-104 "accuses only when the readable read is also the largest" disproved by :455; 7 sourceLabel seam unpinned; 8 check.test.ts comment "under a 404" contradicted by :366; 9 firedRule on unclaimed; 10 spec rule 2 "the readable read did not carry" vs rule 4 union; 11 four stub factories; 12 the parked set).
Lesson (for memory): Task 7's reviewer checked every ADDED sentence against its pin; nobody swept spec/README for UNCHANGED sentences the code had made false. A doc-drift sweep is over the document, not over the diff.
Ruling: R7 - adopt Critical 1's read-scoped rewrite of spec 6.3:515-517 verbatim ("vetoes that read; since plan 1.2 the ladder climbs past it ... A status therefore withholds an accusation from the read it vetoes - but by sending the ladder to the next rung it can relocate the judgement onto a different document, which is the ACCEPTED EXPOSURE 6.6 rule 2 and the README both carry"), keeping the existing history note and adding one clause that this is the paragraph's second correction - spec authority is 6.6 rules 2-3 and the G1/G1' measurement; hyphen-minus only, spec stays 0 non-ASCII - costs two spec sentences if wrong.
Ruling: R8 - adopt Important 2 for README:151-163 verbatim (headline read-scoped, climb sentence appended, Verge measurement kept). For :138-139 the reviewer's clause ("a rung that returns it as text is judged normally") is vacuous - no HTML rung converts a PDF - so the bullet gets the mechanism instead: "reads as `unreachable`: every HTML rung returns the raw bytes, N5 vetoes each read in turn, and the ladder runs out of rungs" - pinned by the N5 climb (check.test.ts:381) and the binary-density tests - costs one README clause if wrong.
Ruling: R9 - adopt Important 3's parenthetical WITHOUT the plan-2 promise ("closing that is plan 2 work" is a commitment nobody has made; the README states what is, not what will be): "(the classifier computes slug overlap and the head markers and gates on neither; it also never compares a read's `finalUrl` with the URL it was asked for, so a redirect away from the citation is observable and, today, unobserved - a gate left unbuilt, not a limit of the signals)"; each new clause gets a pin: headMarkers non-gating `it` beside verdict.test.ts:70, and a check() test with finalUrl on another host/path (matching no CHALLENGE_PATHS pattern), every claim present -> supported, no firedRule - costs two sentences and two small tests if wrong.
Ruling: R10 - minors 5, 6, 7, 8, 10 go into the single fix dispatch (5 and 6 are the two comments guarding a future licence change; 7, 8, 10 are one line each; em-dashes in the reviewer's text become hyphen-minus so src stays 0 non-ASCII); minor 9 (firedRule on an unclaimed result now the readable read's) is PARKED - non-verdict output, unclaimed carries no accusation, the CHANGELOG rule-2 bullet already discloses the winning read's firedRule, and R4's README sentence says it outright; minor 11 (four stub factories) PARKED - test-local duplication the plan chose deliberately (each file must not depend on the corpus); minor 12 stays parked as ruled - costs one CHANGELOG line if 9 is wrong.
Final fix: implementer dispatched (sonnet), BASE 8dfe347, ten items, expected 278 -> 281 tests.
Ruling: R11 - before the workspace is deleted, this ledger is preserved into the repo as docs/superpowers/plans/2026-09-07-plan-1-2-reader-ledger.md (same preamble shape as the plan 1 and 1.1 ledgers, committed as `docs: preserve plan 1.2's execution ledger into the repo`) - the workspace is git-ignored scratch and worktree cleanup destroys it; the rulings R1-R11 with their cost-if-wrong and the final review's proof are the record the next plan reads - costs one doc file if wrong.
Final fix: implementer DONE (sonnet) - commits f453bfe (docs: spec 6.3 + rule 2, README items 2-4, ladder.ts + check.ts comments) + 778066e (test: redirect/finalUrl pin, headMarkers pin, E1 escalation-attestation pin, sourceLabel assertion, :525 comment), 281 passed (controller re-ran: 281, typecheck clean), diff -w stat == diff stat (141+/49-, 7 files), byte counts re-taken by the controller: README 3 / CHANGELOG 0 / spec 0 / calibration 165 / excerpt.ts 57 / check.ts, ladder.ts, three tests 0; NUL 0 everywhere, self-test 1. Item 8 took the brief's fallback (label-free overlap on reads[1] was already 1.0; first call now labelled 'Quarterly Newsletter Digest' at 0.4, second 'The Committee Report' at 1.0; RED shown 'expected 1 to be less than 0.4'). Scoped re-review dispatched (opus - the spec 6.3 rewrite is the merge blocker and must be traced against the code, not read) on review-8dfe347..778066e.diff.
Final fix: scoped re-review (opus) - ALL NINE findings ADDRESSED (Critical 1, Important 2-4, Minors 5-8, 10), every new sentence traced true against the code rather than the brief; no Critical/Important breakage; byte counts re-taken (README 3 / CHANGELOG 0 / spec 0 / calibration 165 / excerpt.ts 57 / rest 0, NUL 0, instrument proved), diff -w stat == diff stat, src/ changes comment-only. Four Minors in the fix diff: (a) README:139-141 mechanism sentence states N5 universal while README:267-273 discloses the uncompressed-PDF evasion of the same shape; (b) read-source.test.ts:37 comment claims the check -> readSource hop it does not pin; (c) the E1 pin does not assert the stub is sub-floor; (d) check.test.ts:566 "on every rung" describes a shape the fixture does not build (curl answers the empty default). Two out-of-scope: spec 6.3:456-460 + decision table :1298 make the N3 twin of Critical 1's claim ("the signature list can withhold an accusation, never supply one") - false via the union (a vetoed read's matches are excluded, so a claim only it carried is `missed` when a readable later rung is judged), PRE-EXISTING at main (union machinery unchanged from 3974d27; N3 fires under 800 chars so it is never rule 2's larger read); README:122 "a vetoed read is never judged on its own" - a vetoed largest read is still passed to verdict() under rule 3 but can only yield unreachable, intent holds.
Ruling: R12 - the four in-diff Minors are fixed by the CONTROLLER (no second dispatch: the skill allows one fix round, and each is one to four lines on text this round introduced), commit 3190de2 (amended from 32ec15b for the conventional prefix), 281 passed, typecheck clean, byte counts re-taken: (a) "- where N5 fires at all: the third of N5's evasions, below, is an uncompressed PDF it does not catch, and that body is judged as text"; (b) comment says only the readSource -> computeSignals hop is pinned and why the other cannot be (slugLabelOverlap never gates, not on CitationResult); (c) STUB hoisted, proseVolume(toText(CHROME)) >= floor and proseVolume(toText(STUB)) < floor asserted; (d) "with nothing readable from a later rung, as here, where curl answers the stub's empty default". These edits are NOT separately reviewed - the diff is small enough to read at the merge menu - costs a re-review of 16+/13- if wrong.
Ruling: R13 - the N3 twin in spec 6.3:456-460 and the decision table :1298 is PARKED, not fixed on this branch: it is false at main, not made false by plan 1.2 (the union is byte-identical to 3974d27), and its correction needs its own pin (node: signature wall under 800 chars carrying claim A, curl: readable body carrying B -> unsupported, missed [A]; same node body without the signature phrase -> supported) that deserves a reviewed dispatch rather than an unreviewed controller edit of the binding spec. Carried as a MANDATORY first item into the next plan's pre-flight (plan 2 harvest), and into the project memory. README:122 stays - costs one more plan of a false N3 sentence in the spec if wrong; the N4 correction on this branch records the same mechanism, so the spec is no longer silent about relocation.
Task 8 (final fix round): complete (commits 8dfe347..3190de2, review clean of critical/important after one fix round + controller minors; 1 pre-existing spec item parked with R13)
